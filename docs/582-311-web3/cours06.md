# Cours 6 | Alpine.js

<!-- **Savoirs :** #11 Sauvegarde côté client (localStorage) · #15 Interactivité · #18 Introduction à un cadriciel JavaScript -->

![](./assets/images/alpinejs-banner.jpg){.w-100}

- Qu'est-ce qu'un cadriciel JS (vs librairie) ; Alpine comme réactivité « saupoudrée » dans le HTML — prolongement naturel des composantes DaisyUI.
- Directives : `x-data` (état), `x-show`/`x-if`, `x-on:click`, `x-model`, `x-text` (expressions JS de base déjà connues).
- Persistance : plugin `$persist` ou lecture/écriture manuelle dans `localStorage`.
